# 带最早执行时间与活跃 Key 去重的任务队列设计

> 本文给出一个面向工程落地的简单设计，不包含建表 SQL、Java 代码、框架配置或部署参数。具体实现应在业务项目中结合现有规范展开。

## 摘要

目标场景具有三个核心特征：

1. 一次可以提交大量任务；
2. 每条任务带有“最早开始执行时间”；
3. 同一业务 `key` 在已有待执行或执行中任务时不能再次加入，任务完成或失败后才允许重新提交。

已知峰值约为每小时 12,000 条，平均约 3.3 条/秒。现有环境已经具备 MySQL、Redis 和 RocketMQ。综合吞吐、并发正确性、可恢复性和维护成本，推荐：

## 推荐方案：MySQL 任务表作为唯一事实源

由 MySQL 同时保存任务状态、执行时间和活跃 `key`，利用数据库唯一约束完成并发去重，工作实例定期批量领取到期任务。Redis 和 RocketMQ 不参与核心正确性，可以在未来有明确性能瓶颈时作为加速层引入。

该方案的主要理由是：

- 当前吞吐远低于 MySQL 任务表的合理承载范围；
- 去重与状态变化可以位于同一个本地事务中；
- 不需要解决 MySQL 与 Redis/RocketMQ 的双写一致性；
- 任务历史、错误原因和执行时间天然可查询、审计；
- 多实例可以通过 MySQL 的跳过锁定行能力并发领取任务。

本文的多实例领取方式以 MySQL 8.0 或更高版本为前提。若具体项目仍使用更早版本，需要替换并发领取策略，但活跃 `key` 的去重模型和状态语义不变。

## 1. 范围与语义

### 1.1 状态

最小状态集合为：

```text
PENDING ──领取──> RUNNING ──成功──> COMPLETED
                         └──失败──> ERROR
```

状态含义：

| 状态 | 含义 | 是否占用业务 key |
| --- | --- | --- |
| `PENDING` | 已接收，尚未到执行时间或等待工作实例领取 | 是 |
| `RUNNING` | 已被某个工作实例领取并开始处理 | 是 |
| `COMPLETED` | 执行成功 | 否 |
| `ERROR` | 执行失败，记录失败信息 | 否 |

`COMPLETED` 和 `ERROR` 都是终态。进入终态后不自动重试；如需再次执行，由调用方重新提交并创建一条新任务。

### 1.2 最早执行时间

“最早开始执行时间”表示任务不得在该时间之前开始，但不承诺在该时间点精确开始：

```text
实际开始时间 >= 最早开始执行时间
```

实际延迟受扫描周期、任务积压、工作实例容量、数据库负载和依赖服务状态影响。调度判断应尽量以数据库时间为准，避免多个应用实例的本地时钟偏差改变结果。

### 1.3 去重范围

去重只覆盖任务的活跃生命周期：

```text
不存在同 key 活跃任务 / 旧任务已终止 -> 允许提交
同 key 任务为 PENDING 或 RUNNING       -> 拒绝新任务
同 key 任务为 COMPLETED 或 ERROR       -> 允许再次提交
```

这里的“重复”仅由规范化后的业务 `key` 判断，任务负载和最早执行时间不同也不会覆盖已经存在的活跃任务。

## 2. 总体结构

```text
批量提交方
    │
    ▼
任务接收服务
    │ 校验、批内去重、写入
    ▼
MySQL 任务表  <────── 查询、审计、告警
    │
    │ 批量领取已到期任务
    ▼
调度/工作实例
    │
    ├── 执行成功 ──> COMPLETED + 释放 key
    └── 执行失败 ──> ERROR     + 释放 key
```

组件职责保持简单：

- **任务接收服务**：校验输入、按顺序完成批内去重、尝试持久化任务并返回接收结果；
- **MySQL 任务表**：保存权威状态，并通过唯一约束裁决并发重复提交；
- **调度/工作实例**：领取到期任务、执行任务、写入终态；
- **恢复任务**：识别异常中断的执行，避免任务永久停留在 `RUNNING`；
- **监控与运维**：观察积压、延迟、错误和异常执行。

## 3. 概念数据模型

任务记录至少需要表达以下信息：

| 信息 | 用途 |
| --- | --- |
| 任务 ID | 每次提交的唯一身份，建议沿用现有 Snowflake 能力 |
| 业务 key | 业务侧用于判断重复的稳定标识 |
| 活跃 key | 仅在活跃状态存在，用于数据库唯一约束 |
| 任务负载或负载引用 | 执行所需数据；大对象宜保存引用而非直接放入任务表 |
| 最早执行时间 | 判断任务何时可以被领取 |
| 状态 | `PENDING`、`RUNNING`、`COMPLETED`、`ERROR` |
| 领取者与租约 | 标识执行实例，并用于发现异常中断 |
| 执行代次或版本 | 防止旧工作实例覆盖新状态 |
| 创建、开始、结束时间 | 审计和延迟指标 |
| 错误类型与摘要 | 失败查询、告警和排障 |

关键设计是一个允许为空的“活跃 key”：

- `PENDING`、`RUNNING` 时，活跃 key 等于业务 `key`；
- `COMPLETED`、`ERROR` 时，活跃 key 为空；
- 活跃 key 受唯一索引约束。

MySQL 唯一索引要求非空值互不重复，同时允许多条空值。这样既能防止两个活跃任务使用同一业务 `key`，又能保留同一 `key` 的多条历史终态记录。

状态进入终态与清空活跃 key 必须在同一次数据库更新中完成，避免任务已经终止但 `key` 尚未释放，或 `key` 已释放但状态仍显示执行中。

## 4. 批量提交

批量提交建议遵循以下逻辑：

1. 校验每条任务的必填信息、业务 `key` 和最早执行时间；
2. 在校验通过的输入中按原始顺序去重，同一 `key` 只保留第一条；
3. 将保留下来的任务分批写入数据库，避免单条超大数据库语句；
4. 由数据库唯一约束处理多个请求或多个服务实例之间的并发冲突；
5. 对每条输入返回“已接收、批内重复、已有活跃任务、输入无效”等明确结果。

推荐批量接口采用逐条判定、部分接收语义：某条任务无效或重复，不影响同批其他合法任务入队。无效输入不参与批内 `key` 竞争，因此同一 `key` 后续第一条合法输入仍可被接收。

不能采用“先查询是否存在，再决定是否插入”作为最终去重手段。两个实例可能同时查询到不存在，再同时插入。查询可以用于改善提示，但最终裁决必须由数据库唯一约束完成。

“只保留前面的”分成两个层次：

- **同一个批次内**：严格按输入顺序保留第一条；
- **并发请求之间**：以数据库唯一约束首先成功写入的任务为准。

若业务要求多个应用实例之间严格按照 HTTP 到达时间决定先后，就需要额外的全局串行入口或序列器。当前场景没有必要承担这项复杂度。

## 5. 到期领取与执行

每个工作实例周期性查找满足以下条件的任务：

- 状态为 `PENDING`；
- 最早执行时间不晚于当前数据库时间。

实例在一个短事务内批量锁定并领取任务。多个实例同时扫描时，应跳过已经被其他事务锁定的记录，避免互相等待，也避免同一任务被两个实例同时领取。领取完成后立即提交事务，具体业务执行必须在事务之外进行，防止长时间持有数据库锁。

领取顺序建议优先考虑最早执行时间，再使用任务 ID 形成稳定次序。但这是尽力而为的顺序，不应把多实例队列定义成严格全局有序队列。

任务执行结束后：

- 成功时，将状态改为 `COMPLETED`，记录结束时间并释放活跃 key；
- 失败时，将状态改为 `ERROR`，记录错误类型、错误摘要和结束时间，并释放活跃 key。

状态更新应校验任务仍由当前工作实例持有，避免过期实例写入错误结果。

## 6. 异常中断与执行语义

工作实例可能在任务变为 `RUNNING` 后宕机，因此不能只依赖正常的成功或失败回调。领取任务时需要记录一个有限租约；执行时间可能超过单个租约周期的任务必须定期续租。后台恢复任务持续检查已经超过租约且没有正常结束的记录。

按照本场景“错误后释放 key、不自动重试”的语义，租约超时的任务应进入 `ERROR`，错误类型标记为“执行结果未知”或“工作实例失联”，并释放活跃 key。

需要注意：

> 数据库只能保证任务状态不会被并发领取破坏，无法单独保证外部业务副作用恰好执行一次。

工作实例可能已经调用外部系统成功，但在更新任务状态前宕机。此时任务最终显示错误，调用方重新提交后可能再次产生相同副作用。因此执行逻辑仍应具备业务幂等能力，例如让下游按稳定的幂等标识拒绝重复操作。

租约代次或 fencing token 可以阻止旧实例继续修改任务状态，但只有下游也识别该代次时，才能阻止旧实例产生外部副作用。

## 7. 为什么暂不使用 Redis 或 RocketMQ

### 7.1 Redis 有序集合

Redis 有序集合适合按时间分数保存待调度任务，Lua 脚本也能原子维护去重键和队列。但业务仍需要在 MySQL 中保留状态和错误历史，于是会出现 Redis 与 MySQL 双写：

- MySQL 成功、Redis 失败时，任务可能永远不被调度；
- Redis 成功、MySQL 失败时，消费者拿到不存在或未提交的任务；
- 故障恢复需要额外的对账和重建流程。

在当前吞吐下，这些复杂度没有对应收益，因此不推荐 Redis 作为权威队列。

### 7.2 RocketMQ 延迟消息

RocketMQ 可以减少数据库轮询，并适合更高吞吐的异步触发。但任务状态和活跃 `key` 仍需由 MySQL 管理，还需要事务消息或 Outbox 保证“任务写入”和“消息发出”的最终一致性。消费者也必须容忍消息重复、过期消息和状态已经变化的消息。

RocketMQ 5.0 延迟消息默认最大延迟范围为 24 小时，投递精度默认约为 1 秒，Broker 故障恢复后也可能产生额外延迟。若项目中任务可能提前数天或数月创建，不能只依赖默认延迟消息能力。

因此，RocketMQ 更适合作为未来的到期唤醒加速层，而不应成为本设计的任务事实源。即使引入消息唤醒，也应保留 MySQL 补偿扫描。

## 8. 容量与运维

虽然瞬时吞吐不高，历史数据仍可能持续增长。若长期保持理论峰值，每年可产生约 1.05 亿条任务记录。因此应明确终态数据的在线保留期，并将更早数据归档或清理。

建议至少观察以下指标：

- 各状态任务数量；
- 已到期但仍为 `PENDING` 的任务数量；
- 最老待执行任务的延迟；
- 从最早执行时间到实际开始时间的分位数；
- 执行成功率、错误率和错误类型分布；
- `RUNNING` 租约超时数量；
- 重复提交拒绝数量；
- 批量接收耗时和数据库领取耗时。

应针对以下情况告警：

- 到期积压持续增长；
- 调度延迟超过业务目标；
- 错误率或租约超时率异常升高；
- 某个业务 `key` 长时间停留在活跃状态；
- 终态数据未按保留策略归档。

## 9. 实施项目需要确定的参数

本文不固定以下实现参数，应在具体项目中通过压测和业务目标确定：

- 扫描周期和允许的调度延迟；
- 单次领取与单次写入的批量大小；
- 工作实例并发数和执行线程数；
- 任务租约时长；
- 任务负载的大小上限和保存位置；
- 业务 `key` 的长度、字符集、大小写和规范化规则；
- 错误摘要长度及敏感信息脱敏规则；
- 终态数据在线保留期与归档方式；
- 接口返回逐条结果还是汇总结果；
- 下游业务幂等标识和保留期限。

## 10. 结论

对于每小时最多约 12,000 条、需要按最早时间执行并在活跃期按业务 `key` 去重的任务，推荐采用：

## MySQL 权威任务表 + 可空活跃 key 唯一约束 + 多实例批量领取

这个设计把最重要的并发规则收敛到单一数据库事务中：活跃任务占用 `key`，成功或错误终态原子释放 `key`，多个工作实例通过跳过锁定行并行消费。Redis 和 RocketMQ 暂不进入核心链路，以避免在当前规模下引入不必要的双写和恢复成本。

## 参考资料

1. Oracle MySQL 8.0 Reference Manual, [Locking Reads][mysql-locking-reads].
2. Oracle MySQL 8.0 Reference Manual, [CREATE INDEX Statement][mysql-create-index].
3. Apache RocketMQ 5.0, [Delay Message][rocketmq-delay].
4. Apache RocketMQ 5.0, [Transaction Message][rocketmq-transaction].
5. Redis Documentation, [Transactions][redis-transactions].

[mysql-locking-reads]: https://docs.oracle.com/cd/E17952_01/mysql-8.0-en/innodb-locking-reads.html
[mysql-create-index]: https://docs.oracle.com/cd/E17952_01/mysql-8.0-en/create-index.html
[rocketmq-delay]: https://rocketmq.apache.org/docs/featureBehavior/02delaymessage/
[rocketmq-transaction]: https://rocketmq.apache.org/docs/featureBehavior/04transactionmessage/
[redis-transactions]: https://redis.io/docs/latest/develop/using-commands/transactions/
