# 带最早执行时间与活跃 Key 去重的任务队列设计

> 本文给出一个面向工程落地的简单设计，不包含建表 SQL、Java 代码、框架配置或部署参数。具体实现应在业务项目中结合现有规范展开。

## 摘要

目标场景具有三个核心特征：

1. 一次可以提交大量任务；
2. 每条任务带有“最早开始执行时间”；
3. 同一业务 `key` 在已有待执行或执行中任务时不能再次加入，任务完成或失败后才允许重新提交。

已知峰值约为每小时 12,000 条，平均约 3.3 条/秒。现有环境已经具备 MongoDB、MySQL、Redis 和 RocketMQ。综合现有基础设施分工、并发正确性、可恢复性和维护成本，推荐：

## 推荐方案：MongoDB 任务集合作为唯一事实源

由 MongoDB 的单个任务文档同时保存任务状态、执行时间、任务负载和活跃 `key`，利用部分唯一索引完成并发去重，工作实例定期原子领取到期任务。MySQL、Redis 和 RocketMQ 不参与核心正确性，可以在未来有明确需求时作为外围集成或加速层引入。

该方案的主要理由是：

- 当前任务属于业务过程记录，与现有 MongoDB 的使用边界一致；
- 任务状态、租约和活跃 `key` 位于同一文档，可通过单文档原子更新完成状态流转；
- 部分唯一索引可以只约束活跃任务，不影响同一 `key` 的终态历史；
- 灵活的文档结构适合承载不同类型的任务负载和错误上下文；
- 不需要解决 MongoDB 与 MySQL、Redis 或 RocketMQ 的双写一致性；
- 当前吞吐不需要依赖分片或专用高吞吐消息队列。

本文假设任务集合运行在生产级 MongoDB 副本集上，关键写入采用多数节点确认，调度、领取和去重判断由主节点裁决。当前规模下建议保持任务集合不分片，避免把全局业务 `key` 唯一性扩展为跨分片协调问题。

## 1. 范围与语义

### 1.1 状态

最小状态集合为：

```text
PENDING ──领取──> RUNNING ──成功──> COMPLETED
                         └──失败──> ERROR
```

状态含义：

| 状态 | 含义 | 是否占用业务 key |
| --- | --- | --- |
| `PENDING` | 已接收，尚未到执行时间或等待工作实例领取 | 是 |
| `RUNNING` | 已被某个工作实例领取并开始处理 | 是 |
| `COMPLETED` | 执行成功 | 否 |
| `ERROR` | 执行失败，记录失败信息 | 否 |

`COMPLETED` 和 `ERROR` 都是终态。进入终态后不自动重试；如需再次执行，由调用方重新提交并创建一条新任务。

### 1.2 最早执行时间

“最早开始执行时间”表示任务不得在该时间之前开始，但不承诺在该时间点精确开始：

```text
实际开始时间 >= 最早开始执行时间
```

实际延迟受扫描周期、任务积压、工作实例容量、数据库负载和依赖服务状态影响。调度时间统一使用 UTC，所有参与调度的实例必须保持可靠的时钟同步，并对可接受的时钟偏差进行监控。

### 1.3 去重范围

去重只覆盖任务的活跃生命周期：

```text
不存在同 key 活跃任务 / 旧任务已终止 -> 允许提交
同 key 任务为 PENDING 或 RUNNING       -> 拒绝新任务
同 key 任务为 COMPLETED 或 ERROR       -> 允许再次提交
```

这里的“重复”仅由规范化后的业务 `key` 判断，任务负载和最早执行时间不同也不会覆盖已经存在的活跃任务。

## 2. 总体结构

```text
批量提交方
    │
    ▼
任务接收服务
    │ 校验、批内去重、写入
    ▼
MongoDB 任务集合 <──── 查询、审计、告警
    │
    │ 循环原子领取已到期任务
    ▼
调度/工作实例
    │
    ├── 执行成功 ──> COMPLETED + 释放 key
    └── 执行失败 ──> ERROR     + 释放 key
```

组件职责保持简单：

- **任务接收服务**：校验输入、按顺序完成批内去重、尝试持久化任务并返回接收结果；
- **MongoDB 任务集合**：保存权威状态，并通过部分唯一索引裁决并发重复提交；
- **调度/工作实例**：领取到期任务、执行任务、写入终态；
- **恢复任务**：识别异常中断的执行，避免任务永久停留在 `RUNNING`；
- **监控与运维**：观察积压、延迟、错误和异常执行。

## 3. 概念数据模型

任务记录至少需要表达以下信息：

| 信息 | 用途 |
| --- | --- |
| 任务 ID | 每次提交的唯一身份，建议沿用现有 Snowflake 能力 |
| 业务 key | 业务侧用于判断重复的稳定标识 |
| 活跃 key | 仅在活跃状态存在，用于部分唯一索引约束 |
| 任务负载或负载引用 | 执行所需数据；大对象宜保存引用而非直接放入任务文档 |
| 最早执行时间 | 判断任务何时可以被领取 |
| 状态 | `PENDING`、`RUNNING`、`COMPLETED`、`ERROR` |
| 领取者与租约 | 标识执行实例，并用于发现异常中断 |
| 执行代次或版本 | 防止旧工作实例覆盖新状态 |
| 创建、开始、结束时间 | 审计和延迟指标 |
| 错误类型与摘要 | 失败查询、告警和排障 |
| 过期时间 | 仅终态任务设置，用于按保留策略进行 TTL 清理 |

关键设计是只在活跃任务中存在的 `activeKey` 字段：

- `PENDING`、`RUNNING` 时，`activeKey` 等于规范化后的业务 `key`；
- `COMPLETED`、`ERROR` 时，从文档中移除 `activeKey`，而不是将它设置为 `null`；
- 在 `activeKey` 字段存在的文档上建立部分唯一索引。

MongoDB 的部分唯一索引只对满足过滤条件的文档实施唯一约束。这样既能防止两个活跃任务使用同一业务 `key`，又能允许已经移除 `activeKey` 的终态文档保留同一业务 `key` 的多次执行历史。

部分唯一索引只能保证已存在的 `activeKey` 不重复，不能单独保证每个活跃任务都携带该字段。集合模式校验和所有应用写路径必须共同维护以下不变量：活跃状态必须存在 `activeKey`，终态必须不存在 `activeKey`。绕过受控写路径的运维操作同样必须遵守该规则。

状态进入终态、移除 `activeKey`、记录结束时间和设置历史过期时间必须在同一次单文档原子更新中完成，避免任务已经终止但 `key` 尚未释放，或 `key` 已释放但状态仍显示执行中。

除去重索引外，还需要围绕“待执行状态 + 最早执行时间 + 稳定次序”设计领取索引，并围绕“执行中状态 + 租约截止时间”设计恢复索引。具体字段顺序应由目标 MongoDB 版本上的查询计划验证决定。

## 4. 批量提交

批量提交建议遵循以下逻辑：

1. 校验每条任务的必填信息、业务 `key` 和最早执行时间；
2. 在校验通过的输入中按原始顺序去重，同一 `key` 只保留第一条；
3. 将保留下来的任务分批写入 MongoDB，避免单次写入携带过多文档；
4. 由部分唯一索引处理多个请求或多个服务实例之间的并发冲突；
5. 对每条输入返回“已接收、批内重复、已有活跃任务、输入无效”等明确结果。

推荐批量接口采用逐条判定、部分接收语义：某条任务无效或重复，不影响同批其他合法任务入队。无效输入不参与批内 `key` 竞争，因此同一 `key` 后续第一条合法输入仍可被接收。

不能采用“先查询是否存在，再决定是否插入”作为最终去重手段。两个实例可能同时查询到不存在，再同时插入。查询可以用于改善提示，但最终裁决必须由 MongoDB 的部分唯一索引完成。

“只保留前面的”分成两个层次：

- **同一个批次内**：严格按输入顺序保留第一条；
- **并发请求之间**：以部分唯一索引首先接受的任务为准。

若业务要求多个应用实例之间严格按照 HTTP 到达时间决定先后，就需要额外的全局串行入口或序列器。当前场景没有必要承担这项复杂度。

## 5. 到期领取与执行

每个工作实例周期性尝试领取满足以下条件的任务：

- 状态为 `PENDING`；
- 最早执行时间不晚于当前调度时间。

每次领取通过一次带状态条件和排序条件的单文档原子更新完成：选中最早到期的 `PENDING` 任务，同时将其改为 `RUNNING`，写入领取者、租约截止时间和执行代次，并把更新后的任务返回给工作实例。因为状态条件和状态修改属于同一个原子操作，某个实例领取成功后，其他实例不再能用 `PENDING` 条件匹配同一任务。

MongoDB 的这一领取方式以单条任务为原子边界，不追求一次原子领取多条任务。工作实例可以循环领取，直到达到本地并发容量或本轮上限。在当前平均约 3.3 条/秒的规模下，逐条原子领取更容易保证正确性，且没有引入多文档事务的必要。

领取顺序建议优先考虑最早执行时间，再使用任务 ID 形成稳定次序。但这是尽力而为的顺序，不应把多实例队列定义成严格全局有序队列。

所有时间统一保存为 UTC。参与调度的实例必须保持可靠的时钟同步，并预留可接受的时钟偏差；“不得提前执行”的严格程度应在具体项目中转化为时钟监控和安全余量。

任务执行结束后：

- 成功时，将状态改为 `COMPLETED`，记录结束时间并移除 `activeKey`；
- 失败时，将状态改为 `ERROR`，记录错误类型、错误摘要和结束时间，并移除 `activeKey`。

状态更新应同时校验任务 ID、`RUNNING` 状态、领取者和执行代次，避免租约过期的旧实例覆盖新状态。状态变化、移除 `activeKey` 和写入结果在同一任务文档中原子完成。

## 6. 异常中断与执行语义

工作实例可能在任务变为 `RUNNING` 后宕机，因此不能只依赖正常的成功或失败回调。领取任务时需要记录一个有限租约；执行时间可能超过单个租约周期的任务必须定期续租。后台恢复任务持续检查已经超过租约且没有正常结束的记录。

按照本场景“错误后释放 key、不自动重试”的语义，租约超时的任务应通过带状态与执行代次条件的单文档更新进入 `ERROR`，错误类型标记为“执行结果未知”或“工作实例失联”，并移除 `activeKey`。

需要注意：

> MongoDB 只能保证单个任务文档的状态不会被并发领取破坏，无法单独保证外部业务副作用恰好执行一次。

工作实例可能已经调用外部系统成功，但在更新任务状态前宕机。此时任务最终显示错误，调用方重新提交后可能再次产生相同副作用。因此执行逻辑仍应具备业务幂等能力，例如让下游按稳定的幂等标识拒绝重复操作。

租约代次或 fencing token 可以阻止旧实例继续修改任务状态，但只有下游也识别该代次时，才能阻止旧实例产生外部副作用。

## 7. 方案取舍

### 7.1 MySQL 任务表

MySQL 同样可以利用唯一索引和并发锁定读取实现该队列，并且一次领取多条任务的事务语义更直接。本设计改用 MongoDB，不是因为 MySQL 无法满足吞吐，而是因为任务被定义为包含负载、状态、错误上下文和生命周期的业务过程记录，与现有 MongoDB 的职责更一致。

MongoDB 的单文档原子更新已经覆盖当前状态流转，不需要为了该任务再同时维护一份 MySQL 状态。保持单一事实源比同时使用 MongoDB 保存过程、MySQL 保存状态更重要。

### 7.2 Redis 有序集合

Redis 有序集合适合按时间分数保存待调度任务，Lua 脚本也能原子维护去重键和队列。但业务仍需要在 MongoDB 中保留任务状态和错误历史，于是会出现 Redis 与 MongoDB 双写：

- MongoDB 成功、Redis 失败时，任务可能永远不被调度；
- Redis 成功、MongoDB 失败时，消费者拿到不存在或未提交的任务；
- 故障恢复需要额外的对账和重建流程。

在当前吞吐下，这些复杂度没有对应收益，因此不推荐 Redis 作为权威队列。

### 7.3 RocketMQ 延迟消息

RocketMQ 可以减少数据库轮询，并适合更高吞吐的异步触发。但任务状态和活跃 `key` 仍需由 MongoDB 管理，还需要 Outbox、事务协调或可靠补偿保证“任务写入”和“消息发出”的最终一致性。消费者也必须容忍消息重复、过期消息和状态已经变化的消息。

RocketMQ 5.0 延迟消息默认最大延迟范围为 24 小时，投递精度默认约为 1 秒，Broker 故障恢复后也可能产生额外延迟。若项目中任务可能提前数天或数月创建，不能只依赖默认延迟消息能力。

因此，RocketMQ 更适合作为未来的到期唤醒加速层，而不应成为本设计的任务事实源。即使引入消息唤醒，也应保留 MongoDB 补偿扫描。

## 8. 容量与运维

虽然瞬时吞吐不高，历史数据仍可能持续增长。若长期保持理论峰值，每年可产生约 1.05 亿条任务记录。因此应明确终态数据的在线保留期，并为终态文档设置过期时间，由 TTL 索引最终清理；需要长期保存的数据应在过期前归档。

MongoDB TTL 删除由后台任务执行，不保证文档在过期时刻立即消失，也可能在高负载或大量文档同时过期时进一步延迟。因此 TTL 只能用于容量治理，不能用于释放业务 `key`、改变状态或触发业务动作。业务 `key` 必须在进入终态时立即通过原子更新释放。

建议至少观察以下指标：

- 各状态任务数量；
- 已到期但仍为 `PENDING` 的任务数量；
- 最老待执行任务的延迟；
- 从最早执行时间到实际开始时间的分位数；
- 执行成功率、错误率和错误类型分布；
- `RUNNING` 租约超时数量；
- 重复提交拒绝数量；
- 批量接收耗时和 MongoDB 原子领取耗时；
- 部分唯一索引冲突数量；
- TTL 已删除文档数和待清理历史规模；
- 主节点切换、复制延迟和写关注超时。

应针对以下情况告警：

- 到期积压持续增长；
- 调度延迟超过业务目标；
- 错误率或租约超时率异常升高；
- 某个业务 `key` 长时间停留在活跃状态；
- 终态数据未按保留策略归档或 TTL 清理持续滞后。

## 9. 实施项目需要确定的参数

本文不固定以下实现参数，应在具体项目中通过压测和业务目标确定：

- 扫描周期和允许的调度延迟；
- 每轮领取上限和单次批量写入大小；
- 工作实例并发数和执行线程数；
- 任务租约时长；
- 任务负载的大小上限和保存位置；
- 业务 `key` 的长度、字符集、大小写和规范化规则；
- MongoDB 写关注、读关注和读偏好；
- 集合的模式校验规则和索引组合；
- 错误摘要长度及敏感信息脱敏规则；
- 终态数据在线保留期、TTL 过期时间与归档方式；
- 接口返回逐条结果还是汇总结果；
- 下游业务幂等标识和保留期限。

## 10. 结论

对于每小时最多约 12,000 条、需要按最早时间执行并在活跃期按业务 `key` 去重的任务，推荐采用：

## MongoDB 权威任务集合 + 活跃 key 部分唯一索引 + 多实例原子领取

这个设计把最重要的并发规则收敛到单个任务文档及其索引中：活跃任务携带 `activeKey`，成功或错误终态原子移除 `activeKey`，多个工作实例通过带状态条件的单文档原子更新并行领取。MySQL、Redis 和 RocketMQ 暂不进入核心链路，以避免在当前规模下引入不必要的双写和恢复成本。

## 参考资料

1. MongoDB Manual, [Partial Indexes][mongodb-partial-indexes].
2. MongoDB Manual, [Atomicity and Transactions][mongodb-atomicity].
3. MongoDB Manual, [`findOneAndUpdate()`][mongodb-find-one-and-update].
4. MongoDB Manual, [TTL Indexes][mongodb-ttl-indexes].
5. Apache RocketMQ 5.0, [Delay Message][rocketmq-delay].
6. Apache RocketMQ 5.0, [Transaction Message][rocketmq-transaction].
7. Redis Documentation, [Transactions][redis-transactions].

[mongodb-partial-indexes]: https://www.mongodb.com/docs/manual/core/index-partial/
[mongodb-atomicity]: https://www.mongodb.com/docs/manual/core/write-operations-atomicity/
[mongodb-find-one-and-update]: https://www.mongodb.com/docs/manual/reference/method/db.collection.findOneAndUpdate/
[mongodb-ttl-indexes]: https://www.mongodb.com/docs/manual/core/index-ttl/
[rocketmq-delay]: https://rocketmq.apache.org/docs/featureBehavior/02delaymessage/
[rocketmq-transaction]: https://rocketmq.apache.org/docs/featureBehavior/04transactionmessage/
[redis-transactions]: https://redis.io/docs/latest/develop/using-commands/transactions/
